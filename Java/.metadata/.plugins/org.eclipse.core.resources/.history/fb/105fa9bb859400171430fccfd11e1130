package memo2;

import java.util.Scanner;
import java.util.List;

public class Controller {
	
	View view;
	
	L//ist<> list = new ArrayList<>();
	
	public static boolean RUN = true;
	
	/*
	List<Integer> list = ArrayList<>();
	Set(Integer> list2 = new HashSet<>();
	
	
	for(int i =0; i<100; i++) {
		Iterator<Integer> interator = list2.iterator();
		
		while(iterator.hasNext()) {
			int num = iterator.next();
			iterator.remove();
		}
	}
	 */
	public Controller() {
		view = new View();
	}
	
	public void run() {
		while(RUN) {
			String std = view.intro();
			switch(std) {
			case "c" :
				Memo memo = view.create();
				memo.date = System.currentTimeMillis();
				break;
			case "r" :
				break;
			case "u" :
				break;
			case "d" :
				break;
			case "l" :
				break;
			case "x" :
				break;
			}
		}
		
	}
		
}

